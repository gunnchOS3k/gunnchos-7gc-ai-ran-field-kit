# HUMAN_ACTION_REQUIRED (minimal)

Generated after maximum Cursor automation. Only items Cursor proved it cannot do.

## SECRET_BOOTSTRAP
1. Create dedicated GitHub App OR fine-scoped read-only PAT for portfolio sibling checkout.
2. Follow exact steps in `gate1/reports/HUMAN_SECRET_BOOTSTRAP_REQUIRED.md`.
3. Why software cannot: no dedicated App PEM / PAT exists locally; interactive `gh` token must not be reused as Actions secret.

## PHYSICAL_FABRICATION
1. Fabricate/assemble ring prototype per `gate1/reports/RING_PROTOTYPE_FABRICATION_PACKET.md` + BOM CSV.
2. Why: Cursor cannot fabricate atoms; packet is `RING_FABRICATION_READY` + `RING_PHYSICAL_PROTOTYPE_BLOCKED`.

## PHYSICAL_CONNECT / PROCUREMENT
1. Acquire/connect preferred gunnchOS boot target (see `GUNNCHOS_PHYSICAL_BOOT_TARGET_SELECTION.md`).
2. Acquire/attach qualifying USB-C/Thunderbolt dock if G1-C3 desired.
3. Why: no boot target, dock, or adb device detected on this host.

## REAL_PLAYER_INTERACTION
1. After apps are launched on a real device/desktop runtime, play one complete core loop per game (Beat Link, Archive of Life, Pedestrian Pursuit, Anime Aggressors Godot path).
2. Why: criterion requires real player interaction; synthetic harnesses are software-only.

## OWNER_EVIDENCE_ACCEPTANCE
1. Review `gate1/reports/EDMUND_FINAL_EVIDENCE_REVIEW_PACKET.md`.
2. For each complete physical bundle, set `decision` in an explicit Edmund decision record (`ACCEPTED` / `REJECTED` / `NEEDS_MORE_EVIDENCE`).
3. Why: Cursor must not auto-accept or impersonate personal review.

## DESTRUCTIVE_DEVICE_CONFIRMATION
1. Only if a dedicated development boot target is acquired and flashing would wipe it — confirm before flash.
2. Why: policy forbids destructive flash without explicit confirmation.

## Not required from Edmund (Cursor already did)
- git fetch/checkout/branching
- secret name inspection
- adb install
- host USB/display/Thunderbolt/adb enumeration
- gunnchAI3k local-runtime smoke + egress rejection tests
- four game software core-loop harnesses
- ring fabrication packet drafting
- boot target selection document
- evidence review packet drafting
