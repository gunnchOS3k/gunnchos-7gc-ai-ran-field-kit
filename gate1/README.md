# Gate 1 contracts

## `game_core_loop.schema.json`

Machine-readable core-loop event schema for Workstream E (each game completes one core loop).

Local copies also live in each game repo under `gate1/contracts/`.

App launch alone is not core-loop completion. Physical playtest remains `PHYSICAL_PLAYTEST_PENDING` until device evidence is attached.
