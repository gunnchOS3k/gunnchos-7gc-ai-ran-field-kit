# research/reproduce — single-packet and suite replay adapters

```bash
python3 -m research.toolkit.reproduce_packet --packet R6G-006
python3 -m research.toolkit.reproduce_packet --packet ALL
make r6g-reproduce
```

Writes digests under `artifacts/r6g/replication/` and optional `LAST_RUN.json` pointers here.
SoA / physical / standardized claims remain false unless externally earned.
