# research/new — scaffolds for new digital experiment packets

Use:

```bash
python3 -m research.toolkit.new_packet --packet R6G-0XX --title "Short title"
```

Scaffolds land in `research/r6g/adoption/packages/` with a pointer here.
Never invent IMPROVED_STATE_OF_ART / PHYSICAL / COMPLIANT claims.
