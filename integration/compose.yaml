services: {}
# Compose placeholder for local multi-repo mounts.
# Gate 2 orchestration is Makefile/Python driven; containers are optional.
volumes:
  contracts: ../contracts
  results: ../results/integrated
