# Troubleshooting

- Schema dir missing: set `GATE2_CONTRACTS_DIR`.
- Strict lock mismatch: check out locked SHAs or rerun in non-strict mode for development.
- Physical collector fails closed without a device — expected.
