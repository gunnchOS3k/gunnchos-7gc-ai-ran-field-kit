# Gate 2 Limitations

- No physical-device dataset is present in `fixtures/controlled/` at automation time.
- NTN latency/capacity/availability values are configured/synthetic assumptions.
- AI-RAN predicted metrics use an analytical model, not field-validated predictors.
- ReadyGary beam actions are optional and unused unless explicitly supplied.
- `GATE_2_PASS` requires external evidence beyond automated software completion.
