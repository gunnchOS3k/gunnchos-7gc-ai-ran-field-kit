# Benchmark Method

SpectrumX `airan benchmark` records policy, input size, repetitions, warmup, runtime distribution, mean/median/p95/stdev, CPU environment, Python version, peak memory, seed, and input hash.
Ablations execute concrete twin mutations and write `ablation_results.csv`.
NTN sensitivity sweeps write `sensitivity_results.csv`.
